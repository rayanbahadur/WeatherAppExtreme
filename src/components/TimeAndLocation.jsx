// TimeAndLocation.js
import React from "react";
import "./TimeAndLocation.css"; // Import your CSS file

const TimeAndLocation = ({ weather: { locDateTime, loc_name, loc_country } }) => {
  return (
    <div>
      </div>
    </div>
  );
};

export default TimeAndLocation;
